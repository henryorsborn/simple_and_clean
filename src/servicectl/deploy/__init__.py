"""Deploy overlays — Azure today, AWS / GCP tomorrow.

Each subdirectory here is a self-contained overlay of files that get layered
on top of a scaffolded service when `--deploy=<x>` matches the directory name.
"""
